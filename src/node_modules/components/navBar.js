import React from 'react'
import {Link, NavLink} from 'react-router-dom'

const NavBar = () => {
    return (
        <nav className="p-3 text-bg-dark">
            <div className="container">
                <div className="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
                    <Link to='/' className='nav-link' exact="true">Home</Link>

                    <ul className="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center nav-pills mb-md-0">
                        <NavLink to='/1' className='nav-link px-2 text-white' >First</NavLink>
                        <NavLink to='/2' className='nav-link px-2 text-white' >Second</NavLink>
                        <NavLink to='/3' className='nav-link px-2 text-white' >Thrid</NavLink>
                    </ul>

                    <div className="text-end">
                        {/*TODO: if user logged in*/}
                        <NavLink to='/login' className='btn btn-outline-light me-2' >Login</NavLink>
                        <NavLink to='/register' className='btn btn-warning' >Sign-up</NavLink>
                    </div>
                </div>
            </div>
        </nav>
    )
}

export default NavBar